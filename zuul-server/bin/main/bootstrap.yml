server:
  port: 8080
  
spring:
  application:
    name: zuul-server
  cloud:    config:      uri: http://localhost:9999

eureka:  client:    service-url:      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}